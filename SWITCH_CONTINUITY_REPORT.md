# Creator Scout 空间切换连续性优化报告

> 后续更新（2026-09-24）：用户确认改为底边距账户入口8px、上边缘约150ms平滑伸缩。下文的最大高度预留、顶部及按钮坐标不变、个人状态额外38px外部空隙均为已被替代的历史方案，不作为当前要求。保留浮窗与局部更新等行为继续有效；最新实现与测试见 `BOTTOM_ANCHOR_REPORT.md`。

日期：2026-09-24。范围：仅现有 `creator-scout-home/` 独立首页中的空间切换及浮窗连续性。264px紧凑尺寸已由用户验收，本次不改字体、间距、颜色、头像、图标或菜单尺寸。不重建项目，不修改或合并V9，不接真实服务。

## 1. 当前行为

| 阶段 | 浮窗 | 背景页面 |
|---|---|---|
| 点击切换 | 同一浮窗保持打开；账号区、空间行及切换按钮保留原节点；显示“切换中…”并禁用功能按钮，允许主动关闭 | 保留原空间，不提前清空、重建侧栏或输入框 |
| 切换成功 | 原位更新空间名称、图标、身份和适用菜单；恢复按钮，不自动关闭、不重播入场动画 | 侧栏项目、最近会话、顶栏与会话输入一次同步到目标空间 |
| 切换失败 | 保留原浮窗和空间信息，恢复按钮，提供失败反馈及重试 | 不重绘，原输入及滚动状态保留 |
| 主动关闭 | 点击外部、再次点击账户入口或Esc关闭；切换期间也可关闭 | 已发出的切换按结果正常完成，回调不自动重开浮窗或抢焦点 |

仍直接在个人与团队之间互切，不恢复空间选择列表。切换成功不弹视觉成功Toast，仅更新当前信息并提供无障碍状态通知。点击其他菜单后的既有动作/收起行为不变；未开放功能仍只显示提示。

## 2. 稳定位置与渲染

- 浮窗打开时测量账号可切换状态中的最大自然高度，用于计算顶部位置；不把最大高度设为固定高度，也不为隐藏的“团队管理”插入空白行。
- 同次打开期间保持浮窗上边缘、账号区、空间区及切换按钮位置稳定。团队创建者切到个人空间时，只让下方菜单与外框下边缘自然缩短38px；反向切换时向下恢复。
- 从个人空间直接打开时也提前考虑较高团队状态的位置，因此个人状态下底边与账户入口之间会比团队状态多38px距离。这是外部位置预留，不是浮窗内部占位。
- 窗口缩小、缩放或可视区域变化时重新限制位置及高度；不强保原坐标而导致出屏。小窗口继续可滚动。
- 切换中及失败事件只更新已有浮窗的忙碌状态，不重建背景DOM。空间成功改变后，背景三个区域各更新一次。
- 外框、账号区、空间行和切换按钮不被删除重建，不重复播放浮窗入场动画。目标空间相关信息和菜单按需更新。

## 3. 焦点、失败和数据保护

键盘打开后焦点进入切换按钮；切换中禁用控件时焦点临时放在仍存在的浮窗容器，成功或失败后回到原切换按钮。Tab/Shift+Tab继续按顺序操作；Esc关闭后回到账户入口。外部点击关闭不抢目标焦点，结果回调也不会重开浮窗。用户在进行中主动重新打开时，显示当前进行中状态，成功后保留这次主动打开的浮窗。

失败重试仍属于切换动作，点击重试不被误判为外部点击而关闭浮窗。再次尝试会清理过期失败提示。保留原有请求序号守卫、重复请求锁定、跨空间草稿隔离和旧草稿兼容。未改变历史消息或项目绑定，未创建真实项目/会话。

## 4. 修改文件

运行代码仅两份：

- `js/app.js`：成功保留浮窗、稳定顶部定位、按页面状态决定背景更新、原位忙碌状态与焦点恢复、失败重试/主动关闭/无障碍反馈。
- `js/ui.js`：新增空间信息及菜单局部更新函数；外置提示提供显式重定位能力，保留清理函数。

测试：更新 `tests/verify.py`、`tests/verify-local-fix.py`、`tests/verify-compact-popover.py` 中与最新交互冲突的旧断言；新增 `tests/verify-switch-continuity.py`。没有放宽草稿隔离、尺寸、焦点、失败状态或出屏断言。

文档：更新 `README.md` 和紧凑化历史报告的后续确认状态，新增本报告。

`css/styles.css`、`js/store.js`、`js/fixtures.js`、`index.html`、侧栏Logo、favicon及V9文件均与本轮开工哈希一致。

已测试的运行代码SHA256：

```text
js/app.js  9add54b7ad916029b4a933f4355afdcff56e491dacce3179492bd8e0e958be8e
js/ui.js   f20fe97f8c60aeeb7e0e06f70713bb9bc6ec60c71dc7dceac416857f09d7cc21
css/styles.css（未改） cb1066e46ad77fd1e212920670984a83735dee01ea1ea5e61ec20979ebbc7780
```

## 5. 实际验证结果

Windows本地隔离Chromium运行，非真实服务环境。

| 检查 | 结果 |
|---|---|
| 切换连续性专项及继承回归 `verify-switch-continuity.py` | 343通过，0失败 |
| 主流程回归 `verify.py` | 76通过，0失败 |
| 两者调用的浏览器内逻辑测试 | 24通过，0失败 |

各层存在覆盖重叠，不累加成独立场景数量。未发现JavaScript运行时错误。

覆盖1440×1024、1280×800；团队/个人连续往返；从个人空间开始切换；原节点身份、切换按钮坐标、背景渲染次数与入场动画次数；失败及重试；切换过程中鼠标外部、Esc、账户入口主动关闭；关闭后成功/失败不重开、不抢焦点；主动重开；320×420、280×240小窗口与resize；四种账户状态及管理员；长中英文名称与邮箱；已有草稿隔离、长文本伸缩、历史会话和退出确认。

1440×1024下实测样例（CSS px）：

| 状态 | 浮窗左上角 | 浮窗宽×高 | 切换按钮左上角 | 按钮宽×高 |
|---|---|---|---|---|
| 团队创建者 | 12,551 | 264×389 | 227,630 | 32×32 |
| 切换到个人，浮窗保留 | 12,551 | 264×351 | 227,630 | 32×32 |
| 再切回团队，浮窗保留 | 12,551 | 264×389 | 227,630 | 32×32 |

受控延迟验证：切换开始后侧栏、顶栏、会话渲染计数均不增加；成功后各增加一次；失败时仍不增加。连续切换未重播入场动画，关键浮窗节点始终相同。

## 6. 入口与截图

可运行入口：`E:\codex\KOL collect\creator-scout-home\index.html`，可直接双击。

复跑命令（在 `creator-scout-home/` 内）：

```text
python -X utf8 tests/verify-switch-continuity.py
python -X utf8 tests/verify.py
```

本轮截图目录：`screenshots/20260924-switch-continuity/`。

- `01-before-team.png`：切换前团队空间，浮窗打开。
- `02-switching-team-to-personal.png`：切换中保留浮窗与原背景。
- `03-after-personal-kept-open.png`：成功进入个人空间，浮窗仍在。
- `04-return-team-kept-open.png`：原位再切回团队。
- `05-failed-kept-open.png`：失败保留与重试反馈。
- `06-personal-start-*.png`：个人状态开始及不同视口。
- `regression-scale/`、`regression-interaction/`：尺寸、长文案与交互回归截图。

证据目录：`tests/evidence/20260924-switch-continuity/`，包括 `before-source.zip`、`protected-sha.json`、`review.diff`、`continuity-results.json`、`main-regression-output.txt`、`main-regression-results.json` 和修改文件记录。

## 7. 边界

本轮交互已实现并通过上述本地检查；实际使用手感仍由用户体验验收。最新Figma首页视觉同步、favicon真实标签栏专项和真实服务接入不在本轮范围，未据此宣称完成。工作区Git不可用，采用开工备份、文件SHA及差异检查保护现场，没有Git提交。
