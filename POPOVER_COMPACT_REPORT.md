# Creator Scout 账户浮窗紧凑化报告

> 后续确认（2026-09-24）：用户已验收本报告中的264px紧凑尺寸。之后新增“空间切换后浮窗保留”的交互要求，已独立实现，见 `SWITCH_CONTINUITY_REPORT.md`。下文保留本轮历史测试与实现记录；旧版切换成功关闭浮窗不再是当前行为。

日期：2026-09-24。范围：仅 `creator-scout-home/` 的账户浮窗、相关提示定位及回归测试。不重建项目，不修改或合并 V9，不接真实服务。用户已暂缓首页 Figma 视觉同步，本次按当前对话确认的 264px 方案执行，不以旧 Figma 的 324/326px 或上一轮 280px 作为回滚目标。

## 1. 本轮结果

在 Windows 本地隔离 Chromium 浏览器中，1440×1024 和 1280×800 均实测团队创建者浮窗为 **264×389 CSS px（包含边框）**；修改前为280×459px。没有使用 zoom 或 scale，未固定浮窗总高度；不同状态按实际内容自然缩短。文字亮度、背景、边框和原有语义色全部保留。

| 项目 | 修改前 | 修改后 |
|---|---:|---:|
| 外框标准宽度，含边框 | 280px | 264px |
| 创建者默认高度 | 459px | 389px |
| 外层内边距 | 10px | 8px |
| 账号区高度 | 70px | 56px |
| 空间区高度 | 76px | 60px |
| 菜单行高度 | 44px | 38px |
| 菜单字号／文字行高 | 14／22px | 14／20px |
| 昵称字号 | 15px | 14px |
| 账号头像 | 40px | 36px |
| 空间头像及切换按钮 | 36px／32px | 保持36px／32px |
| 账号／团队头像占位文字 | 18px／20px | 14px／14px |
| 菜单图标 | 20px | 18px |
| 菜单及账号图文间距 | 12px | 10px |
| 菜单悬停底板圆角 | 8px | 6px |

邮箱／身份仍12px，空间标签仍11px，外框仍8px圆角、无额外投影。账号和空间头像同宽，上下文字左边缘对齐。团队名称和头像文字沿用首页 UI 字体栈；本机实际渲染为 Noto Sans SC，邮箱沿用原数据字体栈，本机仍回退为 Segoe UI，没有安装或分发字体文件。

四态及管理员补充状态的自然高度：

| 状态 | 实测尺寸 |
|---|---|
| 团队创建者／管理员 | 264×389px |
| 个人空间·已有团队 | 264×351px |
| 团队普通成员 | 264×351px |
| 个人空间·无团队 | 264×329px |

长昵称、长邮箱及中英文团队名单行省略并保留全文 title。身份和“团队空间”标签不被长名称挤走；四态标准宽度一致，不随文案长短跳动。极小窗口按可视区限制宽度与高度，允许纵向滚动。

## 2. 切换提示修复

提示从空间信息行移到浮窗外的独立定位层，不再受浮窗滚动和入场动画容器裁切。优先放在外框右侧、间隔8px；右侧不足时选择外框上方或下方，避免覆盖团队名称、标签、身份或其他菜单。

仅切换按钮 hover 或键盘 focus-visible 触发；团队头像、名称及身份不触发。切换中保留“切换中…”反馈。滚动、窗口大小变化和焦点变化会重新定位；重绘、关闭和退出会清理旧提示及监听器。极端视口无法容纳外置提示时不盖住信息，按钮仍保留可访问名称。

没有改变个人／团队直接互切、四态入口规则、草稿保存、新会话空白输入、历史绑定和退出确认行为。

## 3. 修改文件

运行代码仅3个文件：

- `css/styles.css`：只修改账户浮窗样式区域，主内容和侧栏 CSS 与本轮开工备份一致。
- `js/ui.js`：团队名称跟随 UI 字体；新增外置切换提示定位与清理函数。
- `js/app.js`：在浮窗打开、重绘、关闭及退出时装配／清理提示。

文档及测试：`README.md`、本报告、`tests/tests.html`、`tests/verify.py`、`tests/verify-local-fix.py`；新增 `tests/verify-compact-popover.py`。旧断言只按本次授权更新尺寸目标，不放宽交互断言。新截图写到独立目录，不覆盖上一轮截图证据。

`js/store.js`、`js/fixtures.js`、侧栏 Logo、favicon 与 V9 的 SHA256 均与开工基线一致；V9 SHA256：`7b9a6cc771e82e78e5a01be5e11ab708a22c10a647c803c190f376bf8275664d`。

## 4. 实际验证

运行入口：`E:\codex\KOL collect\creator-scout-home\index.html`，可直接双击。也可在该目录运行本地 HTTP 服务。

在 `creator-scout-home/` 中执行：

```text
python -X utf8 tests/verify.py
python -X utf8 tests/verify-compact-popover.py
```

结果：

| 检查层次 | 结果 |
|---|---|
| 浏览器内逻辑检查（由 verify.py 执行） | 24通过，0失败 |
| 主流程回归 verify.py | 75通过，0失败 |
| 紧凑尺度＋提示避让＋既有交互回归 | 180通过，0失败 |

这些层次存在覆盖重叠，不累加成独立用户场景数。检查覆盖1440×1024、1280×800；四态及管理员；长中文／英文团队名、长昵称和邮箱；320×420、300×300、280×240小窗口；按钮提示右置及上／下避让；鼠标和键盘；空间往返、草稿隔离、新空白草稿、长输入增高及回落；切换中、失败重试及退出确认。未发现 JavaScript 运行时错误。

## 5. 截图和证据

原始截图目录：`screenshots/20260924-popover-compact/`。

- `before-after-100pct.png`：修改前280×459与修改后264×389的原始像素等比例对比，未缩放内容。
- `after-home-1440.png`、`after-home-1280.png`：浮窗置于首页整体环境中的截图。
- `after-tooltip-right.png`：右侧外置提示。
- `state-*.png`：四态及管理员实际渲染。
- `tooltip-320x800.png`、`tooltip-320x420.png`、`tooltip-280x240.png`：小窗口提示避让。
- `regression-local/long-*.png`：长文案和长输入回归截图。

证据目录：`tests/evidence/20260924-popover-compact/`。包含 `before-source.zip`、`protected-sha.json`、`compact-results.json`、`actual-fonts.json`、`review.diff`、`main-regression-output.txt` 和 `main-regression-meta.json`。

## 6. 范围与边界

本轮264px紧凑化方案已落地并完成上述本地检查，最终视觉偏好待负责人在实际页面确认。首页最新 Figma 视觉同步、favicon 的真实浏览器标签栏专项验收、真实AI／账号／邮箱／计费接入均不在本轮范围，未借本轮测试宣称完成。未修改全站字体或V9。

当前工作区 Git 状态不可用，本次使用开工压缩备份、文件哈希和逐文件差异复核保护现场，不声称已有 Git 提交。
