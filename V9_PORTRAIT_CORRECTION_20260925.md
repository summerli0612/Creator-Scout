# 项目画像与分屏纠偏（2026-09-25）

## 定位

上一轮把分屏左侧设为弹性占余量，同时对话内容仍限制为 402px。1920px 下左栏约 910px，正文两侧出现大块空白，右侧画像只占 800px。画像面板虽借用了 V9 的字段名称，却没有照搬其编辑状态：输入即写入持久草稿，“取消”无法回退；地区标签在完成前即写入；新增画像直接入列，缺少 V9 的草稿卡和“完成添加”门槛。画像分组的小字还用了未定位的绝对定位，位置可能脱离标题。

## 本轮改动

- 分屏改为侧栏 210px、对话栏 430px、画像栏占满剩余宽度；1280／1440／1920px 的理论画像栏宽分别为 640／800／1280px。右侧面板贴工作区右边缘，画像正文采用 V9 的 960px 阅读上限；两栏分别滚动，输入和确认按钮固定在各自区域底部。
- 画像字段保留完整自然换行，恢复 V9 的“筛选条件”分组、字段来源和完成／取消操作。字段、地区、名称先保持临时编辑值，完成后才写入共享画像草稿；取消时原配置不变。单行编辑支持 Enter 完成，Escape 取消，长文本 textarea 保留换行。
- 新增画像先出现草稿卡，目标达人必填；完成添加后一次性入列，取消不留下空画像。名称留空时按 V9 的目标达人类型自动命名；手动命名后目标达人变化不再改名，清空名称可恢复自动命名。至少保留一张画像的规则仍有效。
- 画像编辑未完成时，底部确认按钮禁用并提示先完成编辑。确认画像仍只打开项目命名弹窗；实际创建仍在命名弹窗提交时发生。
- 修正画像标题、小字及底部按钮的排布，去掉面板内重复的开发说明文案。

V9 文件保持只读；没有加入 V9 的项目列表、两步资料采集、任务创建或 06–11 功能。

## 验证与限制

- JavaScript 语法检查通过；`node tests/repair-00-05-state.cjs` 通过。新增断言覆盖地区原子提交、画像草稿卡尚未入列、完成添加、自动命名和手动名称保护。
- `tests/verify.py` 已按 V9 的取消与新增语义调整，`tests/smoke-round5.py` 现在检查三档宽度下工作区无缝占满、卡片和输入边界；两个 Python 脚本通过语法检查。
- 本轮再次检查任务浏览器入口，仍返回 `Browsers: Error: nodeRepl.fetch request failed`；再次启动 Playwright Chromium 仍返回 `spawn EPERM`。因此本轮无法实际观察用户所见页面、运行浏览器端到端测试或生成新截图。1280／1440／1920px 的实际几何、滚动、按钮可见性和输入法事件仍待浏览器验证；不宣称视觉验收通过。
