--- before/README.md
+++ after/README.md
@@ -2,7 +2,17 @@
 
 KOL Collect 面向出海 KOL 营销团队，覆盖客户项目、达人发现与采集、资料补全及后续运营。本工作区当前用于产品需求、交互原型、设计规范与工程交接。
 
-## 当前确认版本：V9
+## Creator Scout 独立首页：账户浮窗局部定版
+
+更新日期：2026-09-24。独立首页原型已实现；账户浮窗的264px紧凑尺寸及个人／团队空间切换交互已由用户在本地演示中验收通过。当前规则为底边距账户入口8px、约150ms自然高度伸缩、直接互切且成功后保留同一浮窗。无明确新要求，不回退旧尺寸、空间选择列表、成功自动关闭或固定顶部预留空隙方案。
+
+- [独立首页运行入口](./creator-scout-home/index.html)与[运行说明](./creator-scout-home/README.md)。
+- [统一需求规格](./需求说明/登录首页_统一需求规格.md)记录产品行为与边界；[账户浮窗当前规范](./design-spec/pages/account-popover.md)统一尺寸与交互表现。
+- [最终实现、测试与用户验收记录](./creator-scout-home/BOTTOM_ANCHOR_REPORT.md)。首版旧任务仅作历史，不重新执行；后续任务见[工程任务索引](./tasks/README.md)。
+
+本次仅为本地浮窗尺寸与切换交互定版，不代表整个首页最新Figma视觉、完整创建流程、账户内部功能或真实服务验收。首页最新Figma视觉同步暂缓；独立首页不合并V9，V9冻结规则继续有效。
+
+## V9 项目工作区：冻结基线
 
 更新日期：2026-09-21。负责人已确认新建 Agent 任务优化完成，以当前 V9 为基准。后续功能优化进入 V10；V9 保留不覆盖。
 

--- before/design-spec/README.md
+++ after/design-spec/README.md
@@ -1,5 +1,5 @@
 # KOL Collect 设计规范与双 Agent 协作
-更新：2026-09-22
+更新：2026-09-24（账户浮窗定版文档同步）
 
 ## 分工
 - ChatGPT Work：产品负责人、UI 设计协作者、Figma 读取与规范整理、工程任务拆解、最终视觉验收。
@@ -9,8 +9,9 @@
 
 ## 权威与流程
 已确认 Figma → 本目录结构化规范 → tasks/ Patch Task → GLM 实现与截图 → ChatGPT Work 验收 → 必要的新 Patch Task。
-视觉以对应 Figma Frame、Component、Variables 为准；产品行为以用户已确认要求为准。发现冲突必须明确记录，不凭截图推断新的产品规则。
+视觉默认以对应 Figma Frame、Component、Variables 为准；用户已明确批准并验收的局部覆盖优先，写入对应页面规范后不得因旧Figma回滚。产品行为以最新明确决定和统一需求规格为准。发现冲突必须记录，不凭截图推断新规则。本次未重读或修改Figma，不宣称已将本地定版同步回设计稿。
 历史交接用于背景；同一范围的最新明确要求优先。截图辅助核对，不取代结构数据。
+账户浮窗当前没有待执行的尺寸或切换修复；不得恢复324／326／280px旧宽度、空间选择列表、成功自动关闭或最大高度固定顶部方案。完整首页最新Figma视觉同步暂缓，其余功能按明确新授权处理，V9不受此次定版影响。
 GLM 不自行重新设计、美化或简化，不重构无关代码、不更换技术栈。
 
 ## 当前入口
@@ -18,7 +19,9 @@
 - [Creator Scout 品牌规范 · Discovery](foundations/creator-scout-brand.md)：已选定标识的名称、版本、色彩、字体、留白、最小尺寸与导出要求。
 - [后台通用 UI 规则](foundations/backend-ui-rules.md)：后续页面默认沿用的 Mint、文字层级、模块边界、来源行和数据口径规则。
 - [Instagram 达人营销入口设计规范](pages/instagram-agent-entry.md)：记录 00–11 项目创建与首次任务流程，以及 100／50 位预览分支、V9 配置继承和任务成功后的会话边界；产品与工程执行以[登录首页统一需求规格](../需求说明/登录首页_统一需求规格.md)为准。
-- [账户中心设计规范](pages/account-center.md)：个人／团队空间、权益、团队协作、成员、邀请和业务邮箱占位的首轮 Figma 规范。
+- [账户浮窗当前规范](pages/account-popover.md)：本地用户已验收264px紧凑尺寸与空间切换；底边8px、约150ms伸缩、成功保留浮窗。详细参数和交互统一维护于此。
+- [浮窗最终实现与验收记录](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)：既有测试、截图和用户最终确认；[独立首页运行说明](../creator-scout-home/README.md)。不据此认定完整首页、真实服务或账户内部功能完成。
+- [旧账户中心设计规范（历史）](pages/account-center.md)：首轮账户中心设想，仅作追溯，不作为当前浮窗或内部功能开发授权。
 - [V9 确认基线](../V9_BASELINE.md)：新建 Agent 已定版；后续进入 [V10](../AGENT_V10_HANDOFF.md)。
 - [最终收尾验收](../tasks/2026-09-21_new-agent-task-error-feedback-acceptance.md)
 - [新建 Agent 任务规范](pages/new-agent-task.md)

--- before/tasks/README.md
+++ after/tasks/README.md
@@ -2,9 +2,20 @@
 
 ## Creator Scout 独立首页（2026-09-24 最新）
 
-用户已要求交给 GLM 先实现新首页和团队创建者的个人/团队空间交互，暂不合并 V9。执行入口：[独立首页任务单](2026-09-24_creator-scout-home-standalone-glm.md)。完整产品拆分仍留作后续，不一次性全部执行。品牌为 Creator Scout，V9 冻结规则保持不变。本独立页面任务不沿用下文历史 V10 文件命名要求。
+独立首页首版已有实现；账户浮窗的264px紧凑尺寸和个人／团队切换交互已由用户在本地演示中验收通过。当前无待执行的浮窗尺寸／切换修复，不重新新建首页，不回退已验收版本。该结论不代表全首页最新Figma视觉、账户内部功能或真实服务已验收。
 
-更新：2026-09-21。
+| 资料 | 当前用途与状态 |
+|---|---|
+| [统一需求规格](../需求说明/登录首页_统一需求规格.md)／[账户浮窗规范](../design-spec/pages/account-popover.md) | 当前产品规则与浮窗尺寸、交互依据；264px、底边8px、约150ms伸缩、直接互切并保留浮窗 |
+| [独立首页README](../creator-scout-home/README.md)／[底边贴合验收记录](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md) | 运行入口、既有测试证据与用户确认；当前回归为 `verify-bottom-anchor.py`、`verify.py` |
+| [首版任务单](2026-09-24_creator-scout-home-standalone-glm.md)／[首版实现报告](2026-09-24_creator-scout-home-standalone-result.md) | 历史保留，不再作为当前待执行清单或尺寸／切换回滚目标 |
+| [完整产品后续拆分](2026-09-24_home-redesign-glm-handoff.md) | 仅在逐项明确授权后执行；保留已定浮窗行为，真实服务与内部功能另行接入验证 |
+
+首页最新Figma视觉同步暂缓，完整00–11流程不自动开工。品牌为Creator Scout，独立首页不合并V9；本独立页面不沿用下文历史V10文件命名要求。
+
+## V9 历史任务状态
+
+更新：2026-09-21（以下V9定版与冻结规则不变）。
 
 当前 V9 新建 Agent 优化已完成并由负责人确认定版，见 [V9 基线](../V9_BASELINE.md)。本目录没有待继续执行的 V9 新建 Agent Patch。
 

--- before/design-spec/pages/account-popover.md
+++ after/design-spec/pages/account-popover.md
@@ -1,35 +1,82 @@
 # 账户浮窗｜四种空间状态
 
-状态：2026-09-24 用户确认四态静态视觉与入口规则；已实际读取最新 Figma 复核。不代表点击、切换空间或内部功能实现通过。完整产品规则以[统一需求规格第 10 节](../../需求说明/登录首页_统一需求规格.md)为准，复核见[账户浮窗核对记录](../evidence/2026-09-24_account-popover-audit.md)。未授权开发。
+状态：2026-09-24 账户浮窗的紧凑尺寸及个人／团队空间切换交互，已由用户在 `creator-scout-home/` 本地演示中验收通过。本文件是浮窗当前视觉与交互的统一依据；产品行为与业务边界见[统一需求规格第10节](../../需求说明/登录首页_统一需求规格.md)，实现、测试与用户确认见[底边贴合报告](../../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)。
+
+本次定版不代表整个首页最新 Figma 视觉、账户内部功能或真实服务验收通过。无用户明确新要求，不回退旧尺寸、空间选择列表、切换成功自动关闭或固定顶部预留空隙方案。
 
 ## 来源与优先级
 
-- Figma：`vpKV1bb6IHNCkxSp2NPoqV`，页面 `501:2583`，展示画板 `501:2584`。
+- 优先级：用户最新明确决定 → 统一需求规格与本文件的已验收局部规范 → 对应实现／验收记录 → 历史 Figma 与任务单。若后续重新读取的设计节点与此定版冲突，先记录差异，不自行回滚实现。
+- 历史 Figma 来源：`vpKV1bb6IHNCkxSp2NPoqV`，页面 `501:2583`，展示画板 `501:2584`；原读取证据见[账户浮窗核对记录](../evidence/2026-09-24_account-popover-audit.md)。本次为本地验收结果回写，不声称已同步回 Figma 或重新读取最新节点。
 - 2026-09-23 用户补充：账户浮窗须与 `08｜Instagram 达人营销｜项目创建` 的深色后台协调。该上下文修正覆盖本轮最初的浅色浮层方向；不沿用旧账户菜单线框稿。
 - 信息色语义默认沿用[后台通用 UI 规则](../foundations/backend-ui-rules.md)。本轮用户明确指定未读数使用低饱和绿色徽标，作为此浮窗的局部覆盖；身份标签仍为灰阶，不使用高亮 Mint。
-- 深色页面依据 Figma `08` 页默认新会话画板 `306:74` 与[账户中心规范](account-center.md)：主画布 `#0F1115`、面板 `#1B1C22`、边界 `#343741`、主文字 `#E8EAF0`。品牌色不自动替换后台交互色。
+- 延续深色后台：主画布 `#0F1115`、浮层 `#1B1C22`、边界 `#343741`、主文字 `#E8EAF0`。用户明确保留浮窗较亮、成为当前操作重心的表现；不通过降低亮度解决尺寸问题，品牌色不自动替换后台交互色。
 - [旧账户中心规范](account-center.md)仅作历史追溯，不作为本轮开发依据。各入口采用独立页面还是弹窗尚未决定，内部功能与权限另行设计。用户最新账户浮窗交接优先于旧截图与旧说明。
 
 ## 当前视觉
 
-- 中性展示底板 `1900 × 600px`，并列显示四个默认态及创建者切换按钮悬停态。浮窗均宽 `324px`；团队创建者高 `459px`，个人无团队因不展示空间模块高 `383px`，个人已有团队与普通成员高 `415px`，顶部对齐。
-- 深色浮层 `#1B1C22`，`1px #343741` 描边，`8px` 圆角，无额外投影。展示底板为 `#0F1115`。
-- 中文 UI 使用 Noto Sans SC。账号名称 `15px / Medium`；菜单与空间名称 `14px`；邮箱与身份 `12px`。英文空间名称和邮箱使用 Inter。
-- 账号区显示 `40 × 40px`、`8px` 圆角方形头像、林小满、注册邮箱。头像形状对齐 `08｜Instagram 达人营销｜项目创建` 左下角账户入口的 `8px` 圆角方形头像；浮窗原有尺寸不变。空间区以 Northstar Studio 为主信息；名称下方同一行先显示带低饱和底色的 `11px / Medium`“团队空间”标签，再显示无底框的 `12px / Regular`“创建者”身份文字。右侧切换触点保持 `32 × 32px`，空间区高度保持 `76px`。
-- 团队标识与个人空间占位图标均为 `36 × 36px`、`8px` 圆角方形；不改成圆形，不与账号头像强制同尺寸。无团队状态不展示空间图标。
-- 菜单左侧为同规格线性图标；行高 `44px`。功能文字左边缘对齐。账号与空间之间用留白区分，仅在空间下方、账号功能上方、退出登录上方保留 `1px #343741` 分隔线。
-- 菜单顺序：用量与计费、团队管理、业务邮箱；通知（未读 `3`）、个人设置；退出登录。未读徽标保持 `23 × 20px`，用 `#29443B` 背景与 `#C8E9D4` 数字；退出登录文字与图标使用 `#B0B5BF`，不使用危险红色。
-- 默认状态不添加入口右箭头。切换按钮悬停时仅将背景从 `#292E33` 提亮至 `#37413F`，并在按钮右侧显示“切换空间”提示；默认态不显示提示。本轮并列展示这两个静态视觉状态，不展开空间选择。
+| 项目 | 已验收目标 |
+|---|---|
+| 外框标准宽度 | **264px，包含边框**；四态同宽，不按名称长度变化 |
+| 外框 | `1px #343741` 描边、`8px` 圆角、内边距 `8px`、无额外投影 |
+| 账号信息区／空间信息区 | 高 `56px / 60px`；无团队时空间区整块不渲染、不留占位 |
+| 昵称／空间名称 | `14px / 20px / 500`；昵称与左下账户入口同字号 |
+| 菜单文字 | `14px / 20px / 400`，保留主文字亮度 |
+| 邮箱／身份 | `12px / 18px / 400` |
+| 团队空间标签 | `11px / 16px / 500`，内边距 `2px 6px`，标签与身份同排 |
+| 账号头像／空间头像或个人空间图标 | 均为 `36 × 36px`、`8px` 圆角；头像占位文字统一 `14px / 500` |
+| 菜单图标／菜单行 | 图标 `18 × 18px`；菜单行高 `38px`、圆角 `6px` |
+| 图文间距 | 账号区、空间区、菜单均 `10px`；账号与空间文字左边缘对齐 |
+| 切换按钮 | 保持 `32 × 32px`、`8px` 圆角 |
+
+UI文字（含英文团队名和头像占位字）跟随首页 Noto Sans SC 字体栈；邮箱、数字沿用 Inter 数据字体栈。缺失字体时必须记录实际回退，不声称声明字体等于实际字体。既有检查环境中UI为 Noto Sans SC，邮箱回退为 Segoe UI；本次不安装、内嵌或分发字体，不修改V9字体。
+
+账号与空间之间用留白区分；仅在空间下方、账号功能上方、退出登录上方保留细分隔线，不层层嵌卡。未读徽标最小宽 `23px`、高 `20px`，背景 `#29443B`、文字 `#C8E9D4`；退出登录使用 `#B0B5BF`，不用危险红色。默认不添加菜单入口右箭头。
+
+长昵称、邮箱、团队名保持单行省略并提供全文悬停提示；文字区自适应、头像及切换按钮不被挤压。“团队空间”标签与身份完整可见，不随长名称错位。团队名是空间主信息，不重复添加“当前空间”标签。
 
 ## 四态差异
 
-| 状态 | Figma 浮窗节点 | 空间信息与切换 | 空间功能入口 |
+| 状态 | 历史 Figma 节点（非当前尺寸依据） | 空间信息与切换 | 空间功能入口 |
 |---|---|---|---|
 | 团队空间·创建者 | `540:73` | Northstar Studio、团队空间标签、创建者；有切换 | 用量与计费、团队管理、业务邮箱 |
 | 个人空间·无团队 | `558:73` | 不展示空间信息模块或切换按钮；账号与空间功能之间保留一条细分隔线 | 用量与计费、业务邮箱、开启团队协作 |
 | 个人空间·已有团队 | `558:133` | 同一个人空间图标及名称；有切换，不展示团队身份 | 用量与计费、业务邮箱 |
 | 团队空间·普通成员 | `558:193` | Northstar Studio、团队空间标签、普通成员；有切换 | 用量与计费、业务邮箱 |
 
-四态账号区均为林小满与同一注册邮箱。账号功能均为通知（未读 `3`）、个人设置、退出登录。所有新状态只绘制默认态；有切换按钮时沿用基准悬停规则。创建者悬停展示节点为 `552:843`。管理员以后沿用创建者布局并替换身份文字，本轮未另绘制。
+四态账号信息属于同一账号，不随空间改变；账号功能均为通知、个人设置、退出登录。管理员沿用创建者布局并显示管理员身份，已包含于本地测试夹具。历史创建者悬停展示节点为 `552:843`，不据其旧几何值覆盖当前实现。
+
+正常桌面、当前菜单内容下的自然尺寸：创建者／管理员 `264×389px`；个人已有团队及团队普通成员 `264×351px`；个人无团队 `264×329px`。这些高度用于核对，不写成统一固定高度；减少菜单时不保留空白行。
 
 以上姓名、邮箱、团队名称、头像及未读 3 均为样例。账号信息不随空间切换；菜单按团队关系、当前空间和实际角色计算。无团队隐藏整区但保留底层个人空间。退出登录执行前二次确认；通知统一进入通知中心，无独立“我的邀请”。具体业务范围、空间数据隔离与待细化项统一维护在主规格，不从本视觉稿推断额外权限。
+
+## 定位、切换与焦点
+
+正常桌面视口中，浮窗底边始终位于左下账户入口上方 **8px**，宽度保持264px；不按其他空间最大高度预留外部空隙。个人／团队切换带来菜单高度变化时，底边不动、上边缘与切换按钮约 **150ms** 缓出伸缩，不缩放文字或图标，结束恢复自然高度。放弃“按钮屏幕坐标绝对不变”，优先保证贴合账户入口。
+
+| 阶段 | 当前行为 |
+|---|---|
+| 打开 | 点击账户入口打开同一账户菜单；键盘激活后焦点进入可操作项 |
+| 发起切换 | 点击按钮直接个人／团队互切，无空间选择列表、无丢弃确认；先保存原空间输入；仅更新浮窗忙碌状态并锁定功能按钮，背景不提前重绘 |
+| 成功 | 同一个浮窗继续打开，保留账号区与切换按钮节点；原位更新空间信息和菜单，背景项目、最近及会话输入一次同步目标空间；不重播入场动画、不弹视觉成功通知 |
+| 失败 | 原浮窗、原空间、输入及滚动保留，背景不重绘；恢复按钮，提供失败提示及重试；重试不被误判为外部点击 |
+| 主动关闭 | 外部点击、账户入口或Esc均可关闭，包括切换期间；后续结果不得自动重开或抢焦点 |
+
+键盘切换中焦点可临时停在存活的浮窗容器，成功或失败后回同一个切换按钮；Tab／Shift+Tab按菜单顺序操作，Esc关闭后回到账户入口。鼠标外部关闭不抢被点击目标的焦点。退出确认取消后回到仍存在的账户入口。用户在切换中主动重新打开时显示当前忙碌状态，不重新发请求。
+
+## 提示与小窗口
+
+切换按钮背景默认 `#292E33`、悬停 `#37413F`。“切换空间”仅按钮悬停或键盘可见聚焦时出现，头像、名称、身份悬停不触发，默认不常驻；切换中保留“切换中…”反馈。
+
+提示位于浮窗滚动容器外，优先在外框右侧间隔8px；空间不足时向上／下方避让，不覆盖名称、标签、身份或其他菜单。提示跟随实际按钮位置；关闭、退出、过渡中断时清理动画及提示，不能残留在旧坐标。
+
+小窗口按可视区限制宽度、高度并允许纵向滚动，优先保证不出屏和控件可达；极端可视区不为保持坐标或展示提示而遮挡内容。限高滚动状态及“减少动态效果”模式不强行播放高度过渡。过渡中继续Tab、关闭、改变窗口或反向切换应安全结束或衔接，不遗留固定高度。
+
+## 验收范围与历史追溯
+
+用户已验收的是本地浮窗紧凑尺寸及最终切换手感。四态、长文案、键盘、异常与小窗口检查以既有报告和自动化证据为准，不扩大成用户逐项手工验收。后续只在明确新要求下修改本组件；切换中的数据隔离、未发送草稿恢复及退出确认行为不得因无关页面修改而退化。
+
+[底边贴合报告](../../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)保留最终实现、测试及用户确认；[紧凑化报告](../../creator-scout-home/POPOVER_COMPACT_REPORT.md)与[连续性历史报告](../../creator-scout-home/SWITCH_CONTINUITY_REPORT.md)保留各阶段证据。标准回归入口为 `creator-scout-home/tests/verify-bottom-anchor.py` 和 `creator-scout-home/tests/verify.py`；历史Figma布局审计不作为当前定版通过标准。
+
+历史324／326／280px宽度、44px菜单行、15px昵称、40px账号头像、空间选择列表、成功自动关闭、固定顶部并预留最大高度等方案均已被替代，不再作为开发或回滚目标。首页最新Figma视觉同步已暂缓，本次未修改Figma；真实账号、权限、计费、业务邮箱、通知和各入口内部页面仍未接入或未完成，不因本地验收而转为已完成。
+

--- before/需求说明/登录首页_统一需求规格.md
+++ after/需求说明/登录首页_统一需求规格.md
@@ -1,20 +1,19 @@
 # Creator Scout 首页改版：统一需求规格
 
-版本：整合基线 R3 · 2026-09-24。状态：品牌 Creator Scout 已确认；用户要求交给 GLM 独立实现新首页，暂不合并 V9 demo。当前轮次由本任务整理交接，非本任务直接编码。具体首版执行范围以独立首页任务单为准，本文其余完整流程不自动全部进入首版。
-
-本文件只整理需求，不代表工程已实现或验收通过。最新用户交接及后续明确决定优先于本文件；本文件覆盖历史首页文档的冲突描述。后续补充在同一规格中合并，不生成另一套业务结构。
+版本：整合基线 R3.1 · 2026-09-24（账户浮窗定版同步）。状态：独立首页原型已实现；账户浮窗的264px紧凑尺寸及个人／团队空间切换交互，已由用户在本地演示中验收通过。当前运行入口为 `creator-scout-home/index.html`，仍不合并V9。本文其余完整00–11流程、账户内部功能及真实服务不因本次定版自动进入开发或验收完成状态。
+
+本文件统一记录产品规则与已确认状态，不以规格存在推定功能完成。此次只回写已验收的本地浮窗及空间切换，未改运行代码；具体范围见第10节及[底边贴合与验收记录](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)。最新用户明确决定优先；冲突的历史首页任务单不再作为当前执行依据，不新建另一套业务结构。
 
 ## 1. 权威、范围与证据
 
-读取顺序：最新明确决定 → 本规格 → V9_BASELINE.md 与 new-agent-task.md 顶部定版补充 → 最新 Figma 实际节点 → 历史资料。Figma 遗留注释不能推翻最新决定。
+读取顺序：最新明确决定 → 本规格及[账户浮窗当前规范](../design-spec/pages/account-popover.md)等适用的已验收局部规范 → V9_BASELINE.md 与 new-agent-task.md 顶部定版补充 → 对应Figma实际节点 → 历史资料。旧Figma数值与遗留注释不能推翻局部定版；有差异先记录，不自行回滚。
 
 - 工作区：`E:\codex\KOL collect`。已查找工作区及父目录 AGENTS.md，未找到实体文件；执行本次消息提供的 AGENTS.md 规则。
 - V9：`智能挖掘_任务详情_v9_demo.html`，实测 SHA256 与冻结记录一致：`7B9A6CC771E82E78E5A01BE5E11AB708A22C10A647C803C190F376BF8275664D`。
-- [最新 Figma 页面](https://www.figma.com/design/vpKV1bb6IHNCkxSp2NPoqV?node-id=306-73)：实际读取页面子节点、文本及原型动作，并查看 08、11A 渲染图。不是依据旧截图或名称判断。
-- [最近交付节点](https://www.figma.com/design/vpKV1bb6IHNCkxSp2NPoqV?node-id=602-1915)是 11A 的 Shell / Main，其完整画板为 602:1867。
-- 详见 [设计核对记录](../design-spec/evidence/2026-09-24_home-handoff-audit.md) 与 [GLM 任务拆分](../tasks/2026-09-24_home-redesign-glm-handoff.md)。
-
-本轮：将内部工具的入口升级为客户使用的“平台 → 项目”结构，优先 Instagram；通过对话完成首次需求理解、项目画像确认、正式项目创建和首条 Agent 任务提交，随后进入既有 V9 工作区管理业务。
+- 历史R3设计证据：[Figma页面](https://www.figma.com/design/vpKV1bb6IHNCkxSp2NPoqV?node-id=306-73)曾读取子节点、文本、原型动作及08、11A渲染图；[当时交付节点](https://www.figma.com/design/vpKV1bb6IHNCkxSp2NPoqV?node-id=602-1915)是11A的Shell / Main，完整画板为602:1867。
+- 原证据见[设计核对记录](../design-spec/evidence/2026-09-24_home-handoff-audit.md)，完整产品后续范围见[GLM任务拆分](../tasks/2026-09-24_home-redesign-glm-handoff.md)。本次未重新读取或修改Figma；首页最新视觉同步已暂缓，不将本地浮窗验收描述为全首页Figma对齐通过。
+
+完整产品目标（后续范围，非当前原型已实现链路）：将内部工具的入口升级为客户使用的“平台 → 项目”结构，优先 Instagram；通过对话完成首次需求理解、项目画像确认、正式项目创建和首条 Agent 任务提交，随后进入既有 V9 工作区管理业务。
 
 本次新增：账户浮窗四态、入口显隐与顺序、账号与空间归属、空间切换带动首页数据上下文，以及各入口已确认的业务范围（见第 10 节）。这覆盖 R1 对账户入口仅为壳层的限制。
 
@@ -43,7 +42,7 @@
 | 会话 | 首次发送后产生稳定 ID；创建前无 projectId，持有项目草稿；创建成功绑定一个 projectId，持续保留 |
 | 项目 | 与项目助手会话一对一；创建成功后的会话显示名直接取项目名，无独立命名体系 |
 | 画像 | 项目只有一个当前有效版本；可含多张目标达人画像，保留版本记录 |
-| 草稿 | 首次创建阶段的对话与表单共用；按空间、会话、项目及任务类型隔离，禁止跨空间/项目串值；切空间时未保存内容交互待细化 |
+| 草稿 | 首次创建阶段的对话与表单共用；按空间、会话、项目及任务类型隔离。独立首页未发送输入按空间及内部草稿／会话ID保存，切空间保留并恢复，见10.2；完整画像／任务配置的服务持久化及并发提交仍待细化 |
 | Agent 任务 | 属于既有 V9 项目任务体系；记录 projectId、执行配置、确认的画像版本与快照，不创建聊天任务副本 |
 | 消息与修改记录 | 保留原始材料、用户消息、AI 回复及修改事件；历史文字不作为恢复当前草稿的数据源 |
 
@@ -139,9 +138,9 @@
 |---|---|---|
 | C1 | 已确认 | 平台→项目；持续一对一会话；首次任务成功后业务编辑归 V9；全部按本规格执行 |
 | C2 | 已确认 | 11/11A/11B 指定两出口错误连线已移除，有 600:1868 注释；仅该项复验通过 |
-| C3 | 已确认 | 账户浮窗四态与无团队隐藏空间区；账号信息不随空间变，切换实际改变首页数据上下文；详见第 10 节 |
-| D4 | 后续交互细化 | 各账户入口页面/弹窗分别确定；空间选择、加载、失败、未保存处理及退出确认的具体交互不预设 |
-| G7 | 能力缺口 | 账号/空间关系、角色、空间隔离、真实未读数、退出会话及空间切换契约尚需接入；静态四态不代表功能通过 |
+| C3 | 本地局部已验收 | 四态规则保留；用户已验收264px浮窗尺寸与直接个人／团队互切手感。底边8px、约150ms伸缩、成功保留浮窗，详见第10节；不代表生产服务验收 |
+| D4 | 部分关闭／其余待细化 | 独立首页的直接切换、忙碌反馈、失败重试、未发送输入保留及本地退出二次确认已有确定实现，不再列为空间选择待决策。各入口内部页面／权限、完整表单未保存内容及服务注销异常仍待细化 |
+| G7 | 真实服务缺口 | 本地账号／空间为演示数据；真实身份关系、角色鉴权、资源隔离、未读数、服务注销及空间切换契约仍需接入验证，不能由本地通过推定完成 |
 | D1 | 已决策 | 用户确认后台产品品牌为 Creator Scout，后续品牌规范以 Figma 522:73 为准。新首页使用该名称与品牌资产；本次不修改 V9 demo、历史文件名或工作目录 |
 | D2 | 待决策 | Hashtag AI 推荐默认选择政策；继续当前用户选择演示，不设推荐即全选 |
 | D3 | 开发前需明确 | 05–10 正式画像的对话/直接修改，何时写入当前版本。建议即时写共享草稿；进入执行预览前提交有效版本并记录，任务引用该版本快照，无新增确认弹窗。需明确与项目工作区并发修改的版本策略后实现，旧保存/冲突页面不作为依据 |
@@ -175,11 +174,11 @@
 | A15 | 版本与历史 | 当前画像变化不改历史任务快照；保留消息、修改及版本记录 |
 | A16 | 视觉与长内容 | 默认、分屏、预览、错误状态均可读可滚动；底栏无遮挡，无内部说明混入 UI |
 
-以上是未来开发验收标准，本次未运行工程交互测试，也未宣称真实服务链路通过。
-
-## 10. 账户浮窗与空间上下文（R2 新增）
-
-来源：用户本次《首页改版交接｜账户浮窗产品需求》；[最新 Figma](https://www.figma.com/design/vpKV1bb6IHNCkxSp2NPoqV?node-id=501-2583)。复用已确认深色设计，不重做风格，不继承早期账户中心内容。详细视觉参数见 [account-popover.md](../design-spec/pages/account-popover.md)，核对证据见[账户浮窗核对记录](../design-spec/evidence/2026-09-24_account-popover-audit.md)。
+以上A01–A16为完整产品的后续验收标准，不因独立首页存在而全部标通过。当前浮窗与切换的本地检查和用户验收按第10节另列；本次文档同步不重跑或改写历史测试结果，未宣称真实服务链路通过。
+
+## 10. 账户浮窗与空间上下文（R3.1 本地定版）
+
+来源：原账户浮窗需求及2026-09-24本对话中的局部修改、最终用户验收。产品行为在本节统一维护，详细尺寸和交互表现见[account-popover.md](../design-spec/pages/account-popover.md)，实现与验收见[底边贴合报告](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)。[原Figma核对记录](../design-spec/evidence/2026-09-24_account-popover-audit.md)仅作历史证据，旧324／326／280px等值不覆盖当前264px定版。
 
 ### 10.1 结构与四态
 
@@ -205,9 +204,13 @@
 - 每个账号永远保留一个个人空间，最多同时属于一个团队空间；创建或加入团队不删除个人空间，允许切回。切换空间不切登录账号。
 - 已有团队、当前在个人空间：统一个人空间图标及名称，不显示团队名称或身份。团队空间：同一团队共享头像和名称，角色按当前用户显示。
 - “当前空间”仅为需求概念/图层名，不能作为界面标签；界面用个人空间或团队空间。
-- 切换按钮沿用轻量双向水平箭头，仅悬停提示「切换空间」，默认不常驻提示或悬停效果。点击后的选择方式、加载、失败及未保存内容处理均待设计，不默认直接切换，也不默认弹出确认。
-- 切换成功必须让首页项目及相关数据使用目标空间；项目、画像、任务、会话草稿关联和空间配置不得混入另一个空间的数据。最近列表的具体呈现策略待交互细化，但不能通过其显示或打开其他空间未授权数据。
-- 技术建议：缓存键和请求上下文包含 spaceId；服务校验资源归属与成员权限；晚到的旧空间请求不能覆盖新空间视图。草稿留在原空间，不随切换搬迁。此为隔离要求的实现建议，不替代尚未定义的切换 UI。
+- 切换按钮直接在个人空间与所属团队空间之间互切，不恢复空间选择列表，不新增丢弃确认。仅按钮悬停或键盘可见聚焦时显示「切换空间」，悬停头像、名称、身份不触发，默认不常驻；切换中保留必要反馈。
+- 发起切换先保存原空间未发送输入；保存失败则阻止切换并保留输入。处理中仅更新已有浮窗的忙碌状态、禁用重复切换及功能按钮，不提前重绘背景或清空页面。
+- 成功后保留同一浮窗、账号区和切换按钮，局部更新空间信息与适用菜单；首页项目、最近、顶栏及会话输入一次同步目标空间。不自动关闭浮窗，不重播入场动画，不弹视觉成功通知；可提供无障碍成功状态。
+- 失败保留原空间、输入和浮窗，背景不重绘，恢复按钮并提示重试；重试不被当作外部点击。用户主动点击外部、账户入口或按Esc均可关闭，包括处理中；结果回调不得自动重开或抢焦点。
+- 未发送输入按空间＋内部草稿／会话ID隔离。首次进入目标空间为空白，返回恢复该空间上次会话及输入；主动点击Instagram则打开新的空白草稿，不清空其他草稿，不提前进入最近或创建项目。原型使用localStorage，不代表已实现跨设备／服务端持久化。
+- 正常桌面浮窗底边距账户入口8px；菜单增减按当前自然高度约150ms伸缩，上边缘及按钮随高度移动，不再固定顶部或预留最大高度空隙。小窗口限制高度并滚动，减少动态效果模式不强行动画。键盘成功或失败后焦点回同一切换按钮，Esc关闭后回账户入口。
+- 技术隔离要求继续保留：项目、画像、任务、草稿关联和配置必须属于当前空间；缓存键和请求上下文包含spaceId，旧空间晚到响应不能覆盖新空间。本地项目／最近与输入隔离已有演示和测试；画像、任务、计费、配置等真实资源隔离须由服务验证，不能由前端菜单显隐代替。
 - 用户头像与昵称在后续个人设置修改并同步各处，属于账号级。团队头像名称在创建团队时定义，后续修改入口/权限未定义。所有示例身份数据均需替换为真实数据。
 
 ### 10.3 入口业务含义与后续边界
@@ -222,29 +225,32 @@
 | 业务邮箱 | 当前空间的业务邮箱配置 | 查看/使用/修改权限后续定义；可见入口不等于已有写权限 |
 | 通知 | 统一通知中心，无独立我的邀请；浮窗只显示真实未读数量徽标，不展开内容 | 中心承载邀请、邀请过期、加入/移出团队、角色权限变化、Token/余额、版本到期、充值结果及系统通知；零/大数/失败及聚合口径待细化 |
 | 个人设置 | 账号级，所有状态显示，后续含头像昵称编辑 | 内部设置内容及异常状态 |
-| 退出登录 | 所有状态显示；执行前必须二次确认 | 确认样式、进行中/失败与未保存内容处理 |
+| 退出登录 | 所有状态显示；本地演示已实现二次确认、取消回有效入口及保留各空间输入；确认只结束本地演示登录 | 真实会话注销、服务进行中／失败及完整业务表单未保存内容处理 |
 
 各入口究竟用独立页面还是弹窗逐项确定；“进入功能”不代表已经决定路由页面。退出二次确认是本次明确规定的特例，不改变首次配置修改直接生效、无冲突选择弹窗的规则。
 
 ### 10.4 视觉与验收
 
-沿用深色、圆角、描边、文字层级及实际效果；宽约 324px、菜单行高约 44px；高度随内容自然变化，不强行统一。最新读取四态节点 effects 为空，因此不新增额外投影。线性图标与图文对齐一致，少量细线和留白分组，不层层嵌卡，不为所有入口加右箭头。未读用低饱和绿色，退出登录在底部、可点击但不用强红色。
+当前已验收值：浮窗264px宽（含边框）、菜单行高38px、昵称／菜单14px、头像36px、菜单图标18px；外框8px圆角，底边距账户入口8px，约150ms自然高度过渡。保留较亮的主文字、深色背景、描边与现有语义色，不整体缩放、不增加投影或隐藏菜单占位。四态同宽、高度按内容自然变化；长昵称、邮箱、团队名单行省略并可查看全文，标签、身份、头像与切换按钮不被挤压。详细唯一尺寸表及提示避让、焦点要求见[账户浮窗规范](../design-spec/pages/account-popover.md)。
 
 | ID | 场景 | 验收标准 |
 |---|---|---|
 | A17 | 四态菜单 | 与矩阵及顺序逐项一致；管理员复用创建者布局但显示管理员 |
 | A18 | 无团队 | 空间区、图标、文字、按钮和占位全部不存在；开启团队协作存在；底层仍有个人空间 |
 | A19 | 有团队切回个人 | 仅个人空间信息；隐藏团队管理及开启团队协作；账号身份不变 |
-| A20 | 空间切换 | 项目及相关数据/配置实际切换；个人与团队隔离；旧响应不污染目标空间；具体加载失败交互待定义后验收 |
+| A20 | 空间切换 | 直接互切、成功保留同一浮窗；忙碌／失败不重绘背景，成功同步项目／最近／输入；失败可重试；主动关闭后不重开不抢焦点；草稿与旧响应隔离。当前为本地验证，真实资源／配置隔离另验收 |
 | A21 | 用量角色边界 | 普通成员仅自身配额和使用情况，不能读取他人或执行管理操作；服务权限与入口规则一致 |
 | A22 | 信息与通知 | 使用实际账号/团队/角色/未读数据，不写死示例；浮窗不展开通知、不设我的邀请 |
 | A23 | 退出与打开功能 | 退出先二次确认；入口遵循各自已批准承载方式，未设计的内部页面不宣称完成 |
-| A24 | 浮窗视觉 | 复用现稿，默认/悬停区分；无团队内容上移；高度自适应；不添加额外卡片或机械右箭头 |
-
-当前已确认静态视觉与入口规则，尚未验收真实数据、点击行为、空间切换或各功能内部实现。R2 只合并规格，不构成开发授权。
-
-## 11. R3 实施方向
-
-用户现要求：交给 GLM 先独立实现新首页，暂不考虑 V9_demo 代码汇合；以团队空间创建者为主账号，覆盖个人空间与团队空间交互。“企业空间”在本次交接中指既定团队空间，不新增第三种空间类型，UI 继续使用「团队空间」。
-
-执行入口：[Creator Scout 独立首页 GLM 任务单](../tasks/2026-09-24_creator-scout-home-standalone-glm.md)。品牌依据：[Figma 522:73](https://www.figma.com/design/vpKV1bb6IHNCkxSp2NPoqV?node-id=522-73)。本次已只读核对规范文本及矢量交付板，深底横版转轮廓组件为 524:329。新品牌不意味着全后台交互色改成品牌色，中文 UI 字体仍遵循首页规范。
+| A24 | 浮窗视觉 | 264px含边框，菜单行38px；底边贴入口8px，上边缘约150ms收放；无团队整区隐藏无占位；长文案、外置提示、小窗滚动和焦点正确，不回退旧尺寸／固定顶部方案 |
+
+验收分层：用户已确认本地浮窗紧凑尺寸及最终个人／团队切换手感；四态、长文案、键盘和异常等以已记录的自动化证据为准。底边贴合轮次记录416项专项／继承回归、76项主流程及24项页面内逻辑检查通过，各层重叠，不累计为独立场景。本次只同步文档，不改代码、不重新运行这些测试。
+
+不属于本次通过范围：整个首页最新Figma视觉、完整00–11流程、账户内部功能，以及A21／A22等所需的真实鉴权、用量、通知和服务数据。未开放入口在原型中仍只给诚实提示；规格写明未来业务含义不代表功能已接入。
+
+## 11. 当前交付与后续边界
+
+独立首页首版已有实现，以团队空间创建者为默认演示账号；个人／团队直接切换及浮窗尺寸、手感已按第10节定版，不重新执行“新建首页”的旧任务。当前入口见[独立首页README](../creator-scout-home/README.md)，历史过程见[首版任务单](../tasks/2026-09-24_creator-scout-home-standalone-glm.md)及[首版实现报告](../tasks/2026-09-24_creator-scout-home-standalone-result.md)，后两者不是当前回滚目标。
+
+V9继续冻结、不合并。完整产品后续功能按[任务索引](../tasks/README.md)逐项明确授权后再开发；首页最新Figma视觉同步暂缓。“企业空间”仍指既定团队空间，不新增第三种空间类型。品牌依据仍为既有品牌规范及Figma 522:73，深底横版转轮廓组件524:329为历史资产来源；本次未重读或修改品牌稿，不把后台交互色替换成品牌色。
+

--- before/tasks/2026-09-24_creator-scout-home-standalone-glm.md
+++ after/tasks/2026-09-24_creator-scout-home-standalone-glm.md
@@ -1,4 +1,12 @@
 # GLM 执行任务：Creator Scout 独立首页与创建者空间交互
+
+> **历史任务／不再作为当前执行入口（2026-09-24）**：独立首页首版已有实现；用户已验收后续264px紧凑浮窗及个人／团队切换手感。当前以[统一需求规格](../需求说明/登录首页_统一需求规格.md)、[账户浮窗规范](../design-spec/pages/account-popover.md)及[底边贴合验收记录](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)为准，运行入口见[独立首页README](../creator-scout-home/README.md)。
+>
+> 下文第4／8节的324px、44px及第7节“两项空间选择／成功关闭”，以及重新新建首页的执行摘要，均为历史指令，不再执行。当前为264px、38px菜单、底边距入口8px、约150ms伸缩、直接互切且成功保留同一浮窗；旧指令不得用于回滚。下文正文保留原貌供追溯。
+>
+> 关闭本历史实施入口不代表所有首版设计目标已验收：首页最新Figma视觉同步已暂缓，完整00–11流程、账户内部功能及真实服务仍未完成；V9继续冻结、不合并。新工作按[任务索引](README.md)和用户明确新授权执行。
+
+## 原任务正文（历史保留，不作当前执行指令）
 
 日期：2026-09-24。用户已要求交给 GLM 先实现新首页，暂不合并 V9 demo。本单是本次实施入口，不是要求执行历史完整产品任务清单。
 

--- before/tasks/2026-09-24_creator-scout-home-standalone-result.md
+++ after/tasks/2026-09-24_creator-scout-home-standalone-result.md
@@ -1,4 +1,12 @@
 # Creator Scout 独立首页交互原型 · 实现报告
+
+> **首版历史报告／不代表当前实现或验收状态（2026-09-24）**：本报告正文、当时的测试数字、截图路径及设计核对结论原样保留，不改写成后续版本的证据。旧326px浮窗、44px菜单行、成功关闭以及“切换交互待定”等描述已由后续定版替代，不能据此重新执行旧任务或回滚。
+>
+> 当前尺寸与交互见[账户浮窗规范](../design-spec/pages/account-popover.md)，产品边界见[统一需求规格](../需求说明/登录首页_统一需求规格.md)，最终实现、已有测试及用户确认见[底边贴合验收记录](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)。当前本地浮窗为264px、底边距入口8px、约150ms伸缩、直接互切且成功保留。
+>
+> 用户通过的是本地浮窗尺寸及切换手感；本历史报告的“S1–S6全部完成”及Figma对照表不是全首页最新视觉或生产验收。首页最新Figma视觉同步暂缓，真实服务及账户内部功能未完成，V9仍未合并。
+
+## 首版原始记录（历史保留）
 
 日期：2026-09-24。对应任务单：[2026-09-24_creator-scout-home-standalone-glm.md](2026-09-24_creator-scout-home-standalone-glm.md)。
 

--- before/tasks/2026-09-24_home-redesign-glm-handoff.md
+++ after/tasks/2026-09-24_home-redesign-glm-handoff.md
@@ -1,8 +1,8 @@
 # GLM 开发交接：登录首页改版
 
-> R3 最新范围：用户已要求 GLM 先独立实现 Creator Scout 新首页及创建者个人/团队空间交互，暂不合并 V9。请先执行[独立首页任务单](2026-09-24_creator-scout-home-standalone-glm.md)。下文为完整产品后续拆分，不应一次性全部开工；其中 R2“未授权执行”是当时状态，不撤销本次独立首页授权。
+> **R3.1 状态同步（2026-09-24）**：独立首页首版已有实现；本地账户浮窗的264px尺寸与个人／团队切换手感已由用户验收。不要重新执行[首版历史任务](2026-09-24_creator-scout-home-standalone-glm.md)。后续沿用[账户浮窗当前规范](../design-spec/pages/account-popover.md)及[底边贴合验收记录](../creator-scout-home/BOTTOM_ANCHOR_REPORT.md)：底边8px、约150ms伸缩、直接互切、成功保留浮窗。下文完整产品流程须逐项明确授权，不一次性开工，V9不合并。
 
-状态：任务拆分草案 R2 · 2026-09-24，已合并账户浮窗与空间上下文；未授权执行。收到用户明确开始开发后才实施。继续补充需求时先更新统一规格及此任务单。
+状态：完整产品后续任务拆分，保留原T0–T11编号；独立首页已落地的局部不重做。T9／T10的浮窗视觉与本地切换行为已定版，真实身份、资源权限、服务持久化等接入仍待授权和验证；T3–T7完整流程及T11内部功能不因本次文档同步标为完成。首页最新Figma视觉同步暂缓。
 
 唯一产品规格：[登录首页统一需求规格](../需求说明/登录首页_统一需求规格.md)。设计证据：[核对记录](../design-spec/evidence/2026-09-24_home-handoff-audit.md)。V9 主文件冻结，不因本单存在而修改、覆盖或复制生成新版。
 
@@ -19,11 +19,11 @@
 | T6 预览及提交 | T5，任务执行契约 | 将预览内容适配右侧页面，版本校验、幂等提交、失败/未知恢复 | A11/A12/A15；预览最新值；不重建项目；无重复任务 |
 | T7 成功边界与项目衔接 | T2/T6 | 11 消息、历史动作投影、项目画像/智能挖掘路由、咨询模式 | A13/A14；聊天仍可发送但不能修改画像或新建任务；实际状态与 V9 一致 |
 | T8 回归与交付 | T1–T7、T9；T10/T11 按交互细化完成情况 | 有意义的状态/时序/接口与 UI 回归，验收记录 | A01–A24 按静态/交互/服务分层；待定义项不能标通过；真实与模拟分列 |
-| T9 账户浮窗四态 | T1/T2 | 复用 501:2583 深色设计；四态显隐/顺序、实际身份、未读徽标、默认与悬停 | A17–A19/A22/A24；无团队整区不渲染、不留占位；管理员共用布局但正确身份；入口承载方式不擅定 |
-| T10 空间切换与隔离 | T1/T2/T9；先完成 D4 空间选择/加载/失败/未保存交互 | 空间上下文、数据刷新、缓存和请求隔离、当前空间功能参数 | A20；切换不换账号、不只改名称；两空间项目/额度/配置隔离；旧响应不能覆盖新空间；失败和草稿处理按后续定稿 |
+| T9 账户浮窗四态 | T1/T2；沿用已验收局部 | 本地浮窗视觉／四态规则已有实现；264px、38px菜单等以当前浮窗规范为准。后续接真实角色和未读数据 | A17–A19/A22/A24按本地与服务分层；无团队无占位、管理员身份正确；不得按旧501:2583尺寸回滚，不推定内部功能完成 |
+| T10 空间切换与隔离 | T1/T2/T9；沿用10.2已定交互，真实服务契约另核对 | 本地直接互切、忙碌／失败反馈、输入保留已实现；成功保留同一浮窗，底边8px和约150ms伸缩；后续补服务空间上下文、缓存及资源隔离 | A20：切换中／失败不重绘背景，成功同步目标项目／最近／输入；主动关闭后不重开不抢焦点。真实额度／配置／权限隔离单独接入与验证，不再把空间选择UI列为待决策 |
 | T11 入口动作接入 | T9；对应功能交互及接口已定义 | 用量、团队管理、邮箱、通知、个人设置、创建团队的入口派发；退出确认与会话结束 | A21–A23；逐入口接入，不默认全路由；无我的邀请；退出先二次确认；未定义功能不冒充已实现 |
 
-R2 纳入账户浮窗及空间上下文，覆盖 R1 的纯账户壳层限制。T0 不启动早期账户中心整套方案、计费/邮箱/成员管理内部功能、停止任务、其他平台或旧候选任务。入口的已确认可见范围及角色约束必须保留；内部功能另行细化。T9 静态四态可在开发授权后先做，T10/T11 的未定交互必须先定规格。设计缺口已有明确正确行为时按规格实现，不复制错误 Figma 链接；缺口仍须记录。
+T0不启动早期账户中心整套方案、计费／邮箱／成员管理内部功能、停止任务、其他平台或旧候选任务。入口显隐与角色约束继续保留。T9本地组件与T10局部交互已落地并按当前规范沿用，不重做已验收浮窗；T10真实服务接入、T11内部功能和完整表单状态需分别明确契约与授权。已有设计缺口按统一规格记录，不复制错误Figma链接，也不以本地切换通过宣布生产隔离通过。
 
 ## V9 可复用点与必要适配
 
@@ -50,10 +50,10 @@
 | 能力 | 请求关键项 | 返回/保证 | 当前差距 |
 |---|---|---|---|
 | 账号与空间关系 | 登录身份 | 账号头像/昵称/注册邮箱、一个个人空间、至多一个团队关系及角色 | 真实身份及关系服务待核验；示例数据不能硬编码 |
-| 当前空间切换 | 目标 spaceId、登录身份 | 校验可访问目标，返回一致的空间上下文；失败不能出现名称/数据错配 | 选择、加载、失败、未保存交互待细化 |
+| 当前空间切换 | 目标 spaceId、登录身份 | 校验可访问目标，返回一致的空间上下文；失败不能出现名称/数据错配 | 本地UI与未发送输入保留已定；真实鉴权、空间数据提交、服务异常及完整配置草稿契约仍需接入验证 |
 | 空间级资源权限 | spaceId、资源 ID、登录身份 | 项目/额度/配置归属校验；普通成员用量仅自身 | 不从浮窗可见入口推断邮箱修改等未定义权限 |
 | 通知未读 | 登录身份；聚合范围另定 | 真实未读数，邀请等统一通知中心 | 零值、大数、失败展示及统计口径待定义 |
-| 退出登录 | 二次确认后的当前登录会话 | 实际终止登录会话 | 确认呈现、失败处理待定义，不能只跳登录画面 |
+| 退出登录 | 二次确认后的当前登录会话 | 实际终止登录会话 | 本地二次确认与取消返回已有实现；真实注销、服务失败和完整表单未保存处理待接入／细化，不能只跳登录画面 |
 | 会话持久化 | conversationId、平台、消息与客户端消息 ID | 历史、阶段、绑定项目、幂等接收 | 原型未证明具备服务 |
 | 材料处理 | 文件/链接引用、处理请求 ID | 每项状态、可用内容、失败原因与来源 | 需真实上传/解析及失败重试；格式和容量按真实能力约定 |
 | AI 理解/推荐 | 草稿版本、用户 intentSeq、材料上下文 | requestId、字段补丁、推荐依据、已核验入口 | 不能硬编码材料解析、品牌或 URL |
@@ -74,4 +74,4 @@
 3. 至少覆盖异步乱序、重复点击/超时恢复、项目隔离、画像快照、来源无效和关闭重入测试；低影响视觉更改无需机械增加同构测试。
 4. 交付正常与异常流程截图/录屏、状态与接口测试证据；模拟成功必须明确标注验收环境，产品 UI 不露内部说明。
 5. 设计连线复验、原型运行验收、生产服务验收分开记录。不能以设计讨论结束、指定连线修复或本地任务入库认定生产链路完成。
-6. 空间回归至少使用个人空间与团队空间不同项目/额度/邮箱配置；验证创建者在个人空间不见团队管理、已有团队不见开启团队协作、普通成员无管理用量能力，并模拟旧空间响应晚到。未保存草稿的切换策略待细化，不能测试时擅自约定丢弃。
+6. 真实服务接入后的空间回归至少使用个人／团队不同项目、额度及邮箱配置；验证角色权限与旧空间晚到响应。当前本地未发送输入按空间／草稿ID保存，返回恢复、主动新会话为空且不清旧草稿；不得重新约定丢弃。完整画像／任务配置的提交、持久化和并发策略仍按D3及实际服务另行明确，不以演示数据冒充额度或邮箱服务。

--- before/creator-scout-home/README.md
+++ after/creator-scout-home/README.md
@@ -1,6 +1,8 @@
 # Creator Scout 独立首页交互原型
 
-> **2026-09-24 最新：浮窗底边贴合**：264px视觉尺寸保持已验收版本。浮窗底边始终贴在账户入口上方8px；成功切换后继续保留同一浮窗，高度按当前内容约150ms平滑收放，上边缘及切换按钮随高度移动，不再固定顶部或按最大高度预留空隙。小窗口限高滚动，减少动态效果模式跳过伸缩动画。切换中／失败不重绘背景，成功后同步目标空间；主动关闭后回调不重开、不抢焦点。详见 `BOTTOM_ANCHOR_REPORT.md`。本条覆盖旧“顶部及按钮坐标固定”和“成功后关闭浮窗”的要求。
+> **2026-09-24 已验收：账户浮窗尺寸与切换交互**。用户已确认264px紧凑尺寸及最终个人／团队切换手感。底边距账户入口8px，成功切换后保留同一浮窗，高度按当前内容约150ms平滑收放，上边缘及切换按钮随高度移动，不固定顶部或预留最大高度空隙。小窗口限高滚动，减少动态效果模式跳过伸缩。切换中／失败不重绘背景，成功同步目标；主动关闭后不重开、不抢焦点。
+>
+> 当前统一依据：[产品规格第10节](../需求说明/登录首页_统一需求规格.md)和[账户浮窗规范](../design-spec/pages/account-popover.md)；实现、既有测试与用户最终确认见[BOTTOM_ANCHOR_REPORT.md](BOTTOM_ANCHOR_REPORT.md)。无明确新要求，不回退旧尺寸、空间选择列表、成功自动关闭或固定顶部方案。本次仅同步文档，不改代码，不把局部通过扩大为全首页、账户内部功能或真实服务验收。
 
 > **2026-09-24 浮窗紧凑版**：最新用户确认覆盖旧浮窗324/326/280px及44px菜单规范，现为264px含边框、38px菜单、36px头像、18px图标。亮度与业务交互保留。详见 `POPOVER_COMPACT_REPORT.md`。最新首页Figma视觉同步已由用户暂缓，本次不做该项、不宣称已对齐最新Figma。
 
@@ -72,7 +74,7 @@
 ```text
 creator-scout-home/
 ├── index.html          入口页面
-├── css/styles.css      样式（按 Figma 实测值还原）
+├── css/styles.css      样式（历史Figma基础及用户已验收的浮窗局部规范）
 ├── js/
 │   ├── fixtures.js     演示数据（账号 / 两空间项目与会话 / 四态测试夹具，集中管理）
 │   ├── store.js        状态模型（空间隔离、草稿、切换序号守卫、持久化）

--- before/creator-scout-home/BOTTOM_ANCHOR_REPORT.md
+++ after/creator-scout-home/BOTTOM_ANCHOR_REPORT.md
@@ -1,4 +1,6 @@
 # Creator Scout 浮窗底边贴合与高度过渡报告
+
+> **最终状态：本地账户浮窗尺寸与切换交互已由用户验收通过（2026-09-24）。** 实现、测试及截图保留以下交付记录；用户最终确认见第6节。产品行为见[统一需求规格](../需求说明/登录首页_统一需求规格.md)，详细当前参数见[账户浮窗规范](../design-spec/pages/account-popover.md)。
 
 日期：2026-09-24。范围仅为 `creator-scout-home/` 账户浮窗定位及高度过渡。264px宽度、字号、颜色、图标与内部间距保持已验收版本；不改或合并V9，不接入真实服务，不继续首页Figma视觉同步。
 
@@ -50,4 +52,15 @@
 
 证据目录：`tests/evidence/20260924-bottom-anchor/`，包括 `before-source.zip`、`protected-sha.json`、逐帧及断言结果 `bottom-anchor-results.json`、`main-regression-output.txt`、`main-regression-results.json`。Git不可用，采用开工备份、SHA及差异复核，未进行Git提交。
 
-本轮未发现阻塞项；实际使用手感仍由负责人验收。最新Figma首页视觉还原、真实浏览器标签栏favicon专项及真实服务不在本轮范围，未宣称完成。
+交付时本轮未发现阻塞项；后续用户已完成本地尺寸和切换手感验收，见第6节。最新Figma首页视觉还原、真实浏览器标签栏favicon专项、账户内部功能及真实服务均不在本轮通过范围，未宣称完成。
+
+## 6. 用户最终确认与定版维护
+
+2026-09-24，用户先确认紧凑尺寸可接受并已验收，随后对最终底边贴合版本反馈：“很好 我觉得这个版本的切换非常丝滑了”。本次经用户授权，将该确认回写现有规格、任务索引和本报告。
+
+确认范围是 `creator-scout-home/` 本地账户浮窗的紧凑尺寸及个人／团队空间切换体验；不将四态、长文案、键盘、异常等自动化覆盖描述为用户逐项手工验收，也不宣布整个首页或生产服务通过。第4节的416／76／24项是既有运行记录，存在覆盖重叠；本次文档同步未重跑、覆盖或修改这些测试和截图证据。
+
+后续保持：264px含边框、38px菜单行、14px昵称／菜单、36px头像、18px图标；底边距账户入口8px，约150ms按自然高度伸缩；直接互切且成功保留同一浮窗。切换中／失败不重绘背景，成功同步目标，草稿隔离不变，主动关闭后不重开不抢焦点。
+
+无用户明确新要求，不回退324／326／280px、空间选择列表、成功自动关闭或固定顶部预留空隙等历史方案，不在无关功能修改中顺手调整本组件。旧任务与阶段报告保留追溯，不作为重新执行或回滚指令。V9继续冻结，不合并；首页最新Figma视觉同步仍暂缓，真实服务与账户内部功能另行授权与验收。
+
