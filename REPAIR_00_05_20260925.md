# Creator Scout 00–05 本轮修复记录（2026-09-25）

## 修改范围

- `css/styles.css`：AI 主卡片与 820px 阅读列等宽；用户气泡继续右对齐；长材料名称可在弹性容器内省略。分屏铺满侧栏右侧，画像面板贴右边缘并以工作区宽度适配，左侧对话弹性占用余量；分屏内消息与输入框同为 402px 阅读上限。画像字段改为纵向行，移除阅读态限高、两行截断、绝对定位操作区及重复预留宽度。输入聚焦继续只显示外框一层 Mint 边框。
- `js/ui.js`：每条消息输出稳定的 `data-message-id`，供阅读位置恢复使用。
- `js/app.js`：阅读历史时记忆第一条可见消息的 ID 与视口偏移，开关画像重新排版后按锚点恢复；底部状态继续跟随。对话输入框支持 Enter 发送、Shift+Enter 换行，并避开输入法组合与候选确认、按键重复事件；点击和键盘共用原发送入口。
- `tests/repair-00-05-state.cjs`：在无浏览器环境下验证现有状态规则、长画像内容输出及多行编辑控件。
- `tests/smoke-round5.py`：补充 1280／1440／1920px 普通对话卡片、正文、输入框的实际边界断言，分屏贴边与左侧内部对齐断言，以及长画像阅读态全文断言和新截图路径。
- `README.md`：修正过时的固定分屏宽度说明。

V9 画像参考文件未修改；没有扩展 06–11、连接真实服务或提交代码。

## 本轮实际验证

- `node --check js/app.js`、`node --check js/ui.js`、`node --check tests/repair-00-05-state.cjs`：通过。
- `node tests/repair-00-05-state.cjs`：通过。覆盖团队／个人草稿隔离、解析后的共享画像、旧修改意图不能覆盖较新的手动修改、画像新增／删除及最少一张规则、长字段阅读输出与 textarea 编辑、模拟创建失败后名称保留、重试创建成功与防重复、项目只写入所属空间。
- `tests/smoke-round5.py` 已更新并通过 Python 语法检查；其浏览器断言未运行。
- Playwright Chromium 启动失败：`BrowserType.launch: spawn EPERM`。任务提供的浏览器控制入口同时返回 `Browsers: Error: nodeRepl.fetch request failed`，本轮无法取得真实浏览器截图。

## 尚未在浏览器实测

- 1280、1440、1920px 的普通对话与分屏边界、滚动条、关闭和底部按钮可见性，长画像阅读态实际换行及无裁切。
- Enter、Shift+Enter、空输入、发送中长按与中文输入法候选确认的真实键盘行为；中文输入法尤其需要人工输入法实测。
- 20 条以上对话中的底部跟随、历史阅读、回到底部、开关画像后的可见锚点，以及命名弹窗的取消和失败重试 UI、空间切换 UI。

现有 `tests/smoke-round5.py`、`tests/verify-long-conversation.py` 和 `tests/verify.py` 可作为恢复浏览器权限后的回归入口。旧截图仅为历史参考，本轮未生成新截图。上述浏览器项未验收，剩余风险集中在实际几何边界和输入法事件时序。

## 后续修复：打开画像时长对话位置漂移

原实现对所有重绘都用可见消息 ID 与视口偏移恢复位置。打开画像后对话列收窄，前文换行增高，锚点恢复会改变 `scrollTop`。现在记录上次渲染的分屏状态；开关画像且正在阅读历史时恢复原 `scrollTop`，普通消息重绘仍使用消息锚点，原本位于底部时仍跟随底部。

`tests/verify-long-conversation.py` 增加可选视口宽度参数和打开画像后的截图。实际运行 1280、1440、1920px 三档，每档 14 项全部通过，包括打开画像和关闭画像后位置误差不超过 2px、底部跟随、历史阅读、回到底部、会话位置记忆及输入草稿保留。1920px 分屏截图：`screenshots/round5-long-split-1920.png`。`node --check js/app.js`、`python -m py_compile tests/verify-long-conversation.py` 和 `git diff --check` 通过。

本次验收以测试指定的滚动数值为准。分屏导致文本重新换行，因此同一句文字的屏幕坐标可能变化；该项没有独立的逐字视觉位置断言。上文“浏览器未运行”是首次交付时的记录，本节是之后完成的补测。
